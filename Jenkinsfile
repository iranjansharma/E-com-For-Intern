pipeline {
     agent any
     tools{
          node 'Node18'
     }
     stages {
        stage("Build") {
            steps {
                sh "npm install"        
            }
        }
    }
}
